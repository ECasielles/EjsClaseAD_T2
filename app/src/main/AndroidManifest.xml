<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.usuario.ejerciciosad_t2"
    >

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission-sdk-23 android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        >
        <activity android:name=".ui.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.archivos.CodificacionActivity"/>
        <activity android:name=".ui.archivos.AdministradorArchivosActivity"/>
        <activity android:name=".ui.conexion.ConexionAAHCActivity"/>
        <activity android:name=".ui.conexion.ConexionAsincronaActivity"/>
        <activity android:name=".ui.conexion.ConexionHTTPActivity"/>
        <activity android:name=".ui.conexion.ConexionVolleyActivity"/>
        <activity android:name=".ui.conexion.DescargaActivity"/>
        <activity android:name=".ui.conexion.SubirArchivosActivity"/>
    </application>

</manifest>